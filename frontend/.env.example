# Backend API base URL. When unset:
#   - Dev (npm run dev): requests go to /api, proxied to http://localhost:8000
#   - Build: defaults to http://localhost:8000
# Override for a different backend (e.g. deployed URL).
VITE_API_URL=http://localhost:8000
